# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/
*.egg

# Environment — NEVER commit
.env
.env.local
.env.*.local
!.env.example

# Database / local data
*.db
*.sqlite3
chroma_data/
data/
pgdata/

# ML models (large — not committed). Runtime model cache lives in the
# `model-cache` Docker volume (see docker-compose.yml), not a repo path, so this
# is scoped by extension only — a bare `models/` would also match
# backend/app/models/, which is source code.
*.bin
*.safetensors
*.onnx

# Node
node_modules/
.pnp
.pnp.js
frontend/dist/
frontend/.vite/
e2e/node_modules/
e2e/test-results/
e2e/playwright-report/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Test artifacts
.pytest_cache/
htmlcov/
.coverage
coverage.xml
junit*.xml
.playwright-mcp/

# Docker
*.log
logs/

# Claude Code internals
.claude/cache/
.claude/tmp/
.claude/sessions/

# Local orchestration/control files — not part of the public repo
/Master-Prompt.md
/plan.md
/setup.md

# Raw LinkedIn/portfolio capture material — full screenshots and screen recordings
# taken at project completion for Santhosh to review and post himself. Not curated
# README assets (those stay in docs/screenshots/ and are committed); this is the raw
# unedited footage, kept local-only.
/marketing/
